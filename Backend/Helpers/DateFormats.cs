namespace Backend.Helpers
{
    public static class DateFormats
    {
        // Formato por defecto para DateOnly en JSON y reportes
        public const string DefaultDateFormat = "yyyy-MM-dd";
    }
}
